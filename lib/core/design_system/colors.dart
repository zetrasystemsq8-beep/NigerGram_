import 'package:flutter/material.dart';

/// NigerGram Design System - Unified Colors
class NGColors {
  // Backgrounds - Dark Theme
  static const Color background = Color(0xFF0D0D0D);
  static const Color surface = Color(0xFF181818);
  static const Color surfaceLight = Color(0xFF242424);
  static const Color divider = Color(0xFF2E2E2E);

  // Text
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFA0A0A0);
  static const Color textMuted = Color(0xFF6A6A74);

  // Brand Accent - Emerald Green
  static const Color accent = Color(0xFF00C853);
  static const Color accentLight = Color(0xFF00E676);
  static const Color accentDark = Color(0xFF009624);

  // Theme Colors (used in profile themes)
  static const Color themeGold = Color(0xFFFFD700);
  static const Color themeBlue = Color(0xFF0088FF);
  static const Color themePurple = Color(0xFF8B00FF);
  static const Color themeGreen = Color(0xFF00C853);

  // Status & Badges
  static const Color online = Color(0xFF32D74B);
  static const Color verified = Color(0xFF1DA1F2);
  static const Color premium = Color(0xFFFFD700);

  // Functional Colors
  static const Color like = Color(0xFFFF2D55);
  static const Color info = Color(0xFF2196F3);
  static const Color warning = Color(0xFFFFD600);

  // Status
  static const Color success = Color(0xFF00C853);
  static const Color error = Color(0xFFFF1744);

  // Legacy support
  static const Color accentGold = Color(0xFFFFD700);
  static const Color accentBlue = Color(0xFF0088FF);
  static const Color accentPurple = Color(0xFF8B00FF);
  static const Color accentGreen = Color(0xFF00C853);

  // Keep for backward compatibility
  static const Color gold = Color(0xFFFFD700);
}

// Keep for files that use them directly
const black = Colors.black;
const black54 = Colors.black54;
const white = Colors.white;
const blue = Colors.blue;
const orange = Colors.orange;
const green = Colors.green;
const red = Colors.red;
const transparent = Colors.transparent;
